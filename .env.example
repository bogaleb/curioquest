# Copy to .env.local. Get these from your Supabase project's Connect dialog.
# Keep these blank until a real project is selected. Keep all values in this committed template blank.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only: never prefix this with NEXT_PUBLIC_ or put it in client code.
SUPABASE_SECRET_KEY=
