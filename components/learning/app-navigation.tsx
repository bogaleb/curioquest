'use client';
import {Compass,Map,Gamepad2,Users,Leaf,Star,Palette,FlaskConical,BookOpen,Heart} from 'lucide-react';
const destinations=[{id:'adventure',label:'My adventure',icon:Compass},{id:'games',label:'Game Zone',icon:Gamepad2},{id:'studio',label:'Creative Studio',icon:Palette},{id:'worlds',label:'My worlds',icon:Map},{id:'garden',label:'Build Lab',icon:Leaf},{id:'team',label:'Team Quest',icon:Users},{id:'rewards',label:'Treasure chest',icon:Star}];
export function AppNavigation({view,onNavigate,stars,faith}:{view:string;onNavigate:(view:string)=>void;faith:boolean;stars:number}){const links=[...destinations.slice(0,3),{id:'science',label:'Discovery Lab',icon:FlaskConical},{id:'stories',label:'Story Harbor',icon:BookOpen},...destinations.slice(3),...(faith?[{id:'faith',label:'Faith & Bible',icon:Heart}]:[])];return <nav aria-label="Main navigation">{links.map(item=><button key={item.id} className={`nav-item ${view===item.id?'active':''}`} aria-current={view===item.id?'page':undefined} onClick={()=>onNavigate(item.id)}><item.icon/>{item.label}{item.id==='rewards'&&stars>0&&<span className="nav-count">{stars}</span>}</button>)}</nav>;}
